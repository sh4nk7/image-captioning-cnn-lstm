# Baseline CPU configuration for the main reproducible experiment

data:
  images_dir: "data/Flickr8k_Dataset/"
  captions_train: "data/Flickr8k_text/train.csv"
  captions_val: "data/Flickr8k_text/val.csv"
  vocab_path: "data/vocab.json"
  img_size: 299

model:
  embed_dim: 300
  hidden_dim: 512
  num_layers: 1
  dropout: 0.3
  train_cnn: false
  cnn_lr_factor: 0.1

train:
  epochs: 5
  batch_size: 16
  learning_rate: 0.001
  label_smoothing: 0.1
  lr_factor: 0.5
  lr_patience: 1
  ckpt_dir: "checkpoints/baseline_cpu/"
  log_dir: "runs/tensorboard/baseline_cpu/"
  num_workers: 0
  log_interval: 50
  use_scheduled_sampling: true
  teacher_forcing_start: 0.7
  teacher_forcing_end: 0.3

seed: 42
device: "cpu"

eval:
  captions_json: "data/Flickr8k_text/test_captions.json"
  output_json: "results/baseline_cpu_predictions.json"
  max_len: 20
  top_k: 1
